language: python
python:
        - "3.5"
install:
        - pip install coverage
        - pip install pytest-cov
        - pip install colored
script:
        - py.test --cov-report term --cov=. test_rpn.py
        - py.test --cov-report html --cov=. test_rpn.py
